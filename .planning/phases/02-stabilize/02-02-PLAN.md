---
phase: 02-stabilize
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - .planning/phases/02-stabilize/backups/
autonomous: false
requirements:
  - RULE-06
  - INTG-01
  - INTG-02
  - INTG-03

must_haves:
  truths:
    - "Live Firestore rules match the merged firestore.rules from Plan 01"
    - "Live Storage rules match the canonical storage.rules from Plan 01"
    - "A scholar can RSVP to an event without seeing a blank screen"
    - "Squarespace website photo carousels load coach/scholar headshots without 403"
    - "Salesforce contact photos load correctly"
    - "Backup of pre-deployment live rules exists for rollback"
  artifacts:
    - path: ".planning/phases/02-stabilize/backups/pre-deploy-firestore.rules"
      provides: "Backup of live Firestore rules before deployment"
      min_lines: 10
    - path: ".planning/phases/02-stabilize/backups/pre-deploy-storage.rules"
      provides: "Backup of live Storage rules before deployment"
      min_lines: 5
  key_links:
    - from: "firestore.rules"
      to: "Firebase production Firestore"
      via: "firebase deploy --only firestore:rules"
      pattern: "Successfully deployed"
    - from: "storage.rules"
      to: "Firebase production Storage"
      via: "firebase deploy --only storage"
      pattern: "Successfully deployed"

user_setup: []
---

<objective>
Back up current live rules, deploy the merged Firestore and Storage rules to production, and verify that PWA event registration, external photo integrations, and native app compatibility all work.

Purpose: The merged rules from Plan 01 are ready to deploy. This plan executes the deployment and validates that the three reported symptoms (blank RSVP screen, 403 photo errors on Squarespace/Salesforce) are resolved.

Output: Live Firebase rules updated, backups saved, Jim's verification confirms all integrations working.
</objective>

<execution_context>
@/Users/jimrodarte/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jimrodarte/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-stabilize/02-CONTEXT.md
@.planning/phases/02-stabilize/02-RESEARCH.md
@.planning/phases/02-stabilize/02-01-SUMMARY.md

<interfaces>
<!-- Files to deploy (created by Plan 01) -->
@firestore.rules
@storage.rules
@firebase.json
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Back up live rules and deploy merged rules to production</name>
  <files>.planning/phases/02-stabilize/backups/pre-deploy-firestore.rules, .planning/phases/02-stabilize/backups/pre-deploy-storage.rules</files>
  <action>
**Step 1: Create backup directory.**
```bash
mkdir -p .planning/phases/02-stabilize/backups
```

**Step 2: Back up current live rules.**

For Firestore rules, try `firebase firestore:get` first. If that command doesn't exist or fails, copy from `/tmp/live-firestore.rules` (fetched during Phase 1 diagnosis). Save to `.planning/phases/02-stabilize/backups/pre-deploy-firestore.rules`.

For Storage rules, copy from `/tmp/live-storage.rules` (fetched during Phase 1, identical to native app's storage.rules). Save to `.planning/phases/02-stabilize/backups/pre-deploy-storage.rules`.

Add a comment header to each backup file with the date and purpose: "Pre-deployment backup -- redeploy this file to rollback."

**Step 3: Deploy Firestore rules.**
```bash
firebase deploy --only firestore:rules
```
Confirm output shows successful deployment. If deployment fails, stop and report the error -- do NOT proceed to Storage deployment.

**Step 4: Deploy Storage rules.**
```bash
firebase deploy --only storage
```
Confirm output shows successful deployment.

**Step 5: Verify Cloud Functions are still deployed (INTG-03).**
Run `curl -s -o /dev/null -w "%{http_code}" https://us-central1-level-up-app-c9f47.cloudfunctions.net/getPhoto` to confirm the endpoint responds (expect 200 or 400, not 404). Do the same for `coaches` and `students` endpoints. These functions should be unaffected by rule changes (they use Admin SDK), but confirm they're still accessible.

**What to avoid and WHY:**
- Do NOT use `firebase deploy` without `--only` flags -- this would redeploy hosting and functions, which is out of scope and risky.
- Do NOT skip the backup step -- rollback plan depends on having pre-deployment rules saved.
- Do NOT deploy from any directory other than `Level-Up-App/` -- per user decision, all deployments originate from this repo only.

**Rollback plan (if needed):** Copy the backup files to the repo root, overwriting `firestore.rules` and `storage.rules`, then run the same deploy commands. This takes ~30 seconds.
  </action>
  <verify>
    <automated>test -f .planning/phases/02-stabilize/backups/pre-deploy-firestore.rules && test -f .planning/phases/02-stabilize/backups/pre-deploy-storage.rules && echo "Backups exist" && curl -s -o /dev/null -w "%{http_code}" https://us-central1-level-up-app-c9f47.cloudfunctions.net/getPhoto</automated>
  </verify>
  <done>
Backups of pre-deployment rules saved. Both Firestore and Storage rules deployed successfully to production. Cloud Function endpoints (getPhoto, coaches, students) confirmed accessible.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Jim verifies RSVP flow, photo integrations, and admin functionality</name>
  <files>none</files>
  <action>
Human verification checkpoint. Jim tests the deployed rules with admin and coach accounts to confirm all three reported symptoms are resolved and external integrations work.
  </action>
  <verify>Jim confirms all 5 tests pass (or describes failures for rollback/fix).</verify>
  <done>Jim approves: RSVP works for admin and coach, Squarespace photos load, Salesforce photos load, admin user management works.</done>
  <what-built>
Merged Firebase Security Rules deployed to production. Changes:
- Firestore: restored admin overrides, dual isAdmin() check, RSVP rate limiting with admin bypass, notification_tokens rules, phone validation, matches update, resource role validation, wildcard catch-all, registrationCodes admin-only (per your decision)
- Storage: public read on user profile photo paths (for Squarespace/Salesforce), auth-required on event images
- Cloud Functions: unchanged (confirmed still deployed and accessible)
  </what-built>
  <how-to-verify>
**Test 1: RSVP Flow (admin account -- RULE-05, RULE-06)**
1. Log into PWA at https://app.levelupcincinnati.org with your admin account
2. Open any event with registration enabled
3. Click Register / RSVP
4. Confirm the RSVP flow completes without a blank screen
5. Verify your RSVP appears in the event's registration list

**Test 2: RSVP Flow (coach account -- RULE-05, RULE-06)**
1. Log in with a coach account
2. RSVP to the same or different event
3. Confirm RSVP completes successfully

**Test 3: Squarespace Photo Embed (INTG-01)**
1. Visit the Squarespace website page that shows coach/scholar photo carousels
2. Confirm headshot photos load without 403 errors
3. Check that photos display correctly (not broken image icons)

**Test 4: Salesforce Photo Integration (INTG-02)**
1. Open a Salesforce contact record that should display a photo
2. Confirm the contact photo loads correctly

**Test 5: Admin User Management (secondary -- restored by merged rules)**
1. As admin, try editing another user's profile
2. Confirm the edit saves successfully

**If any test fails:** Describe what happened. Rollback is available -- redeploying the backed-up rules takes ~30 seconds.
  </how-to-verify>
  <resume-signal>Type "approved" if all tests pass, or describe which tests failed and what you observed.</resume-signal>
</task>

</tasks>

<verification>
1. Backup files exist at `.planning/phases/02-stabilize/backups/`
2. `firebase deploy --only firestore:rules` succeeded
3. `firebase deploy --only storage` succeeded
4. Cloud Function endpoints respond (getPhoto, coaches, students)
5. Jim confirms: RSVP works (admin + coach), Squarespace photos load, Salesforce photos load
</verification>

<success_criteria>
- A scholar can open an event, click Register, complete the RSVP flow, and see their registration confirmed -- no blank screen
- The Squarespace website photo embed loads coach/scholar headshots without a 403 error
- The Salesforce integration pulls user photos correctly
- Backup rules exist for rollback if needed
- All deployed rule changes validated against native app compatibility (superset approach guarantees this)
</success_criteria>

<output>
After completion, create `.planning/phases/02-stabilize/02-02-SUMMARY.md`
</output>
